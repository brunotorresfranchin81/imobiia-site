/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'uszopnkydweixqgyfynj.supabase.co' },
    ],
  },
}

export default nextConfig
